#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Correct)
 */

int main(void)

{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)

	{
	if ("ch == 'q' \n")
		continue;

		else if ("ch == 'e' \n")
			continue;

		{	putchar(ch); }	
		putchar('\n');
	}
	return (0);
}
