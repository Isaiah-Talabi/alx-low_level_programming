#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Correct)
 */

int main(void)

{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		'\n';
	{
	if (ch == 'q')
		continue '\n';

		else if (ch == 'e')
			continue;

		{	putchar(ch); }
		ch++;
		putchar('\n');
	}
	return (0);
}
