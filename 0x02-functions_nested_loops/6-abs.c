#include "main.h"

/**
 * _abs - computes absolute values of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */

int_abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
